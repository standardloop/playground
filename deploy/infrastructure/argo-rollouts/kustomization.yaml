---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: argo-rollouts

resources:
  - namespace.yaml
  - release.yaml

labels:
  - pairs:
      environment: playground
    # includeSelectors: true  # Updates matchLabels/selectors
    # includeTemplates: true  # Updates pod template labels
